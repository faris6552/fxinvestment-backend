# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# Aiven MySQL Configuration with SSL - Using Environment Variables
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql-51453c-farisshiku-4097.l.aivencloud.com}:${DB_PORT:18968}/${DB_NAME:defaultdb}?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_vdF-cbxC1-CLtU5Iwrf}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Hikari Connection Pool
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# Disable DevTools in production
spring.devtools.add-properties=false